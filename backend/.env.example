# Environment Variables Template for Production
# Copy this file to .env and update with your actual values

# Database Configuration (Update with your production database)
DB_USER=your_production_db_user
DB_HOST=your_production_db_host
DB_NAME=your_production_db_name
DB_PASS=your_production_db_password
DB_PORT=5432

# Session Configuration (IMPORTANT: Change this to a secure random string)
SESSION_SECRET=change-this-to-a-secure-32-character-random-string

# Server Configuration
PORT=3000
NODE_ENV=production

# Example of secure session secret generation:
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
