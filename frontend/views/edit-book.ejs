
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book - Book Notes</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>
<body class="app-container">
    <nav class="navbar">
        <div class="nav-content">
            <a href="/" class="nav-brand">📚 Book Notes</a>
            <div class="nav-user">
                <span class="nav-username">Welcome, <%= username %>!</span>
                <a href="/logout" class="btn-logout">Sign Out</a>
            </div>
        </div>
    </nav>

    <main class="main-content">
        <header class="page-header">
            <h1 class="page-title">Edit Book</h1>
            <p class="page-subtitle">Update your book details</p>
        </header>

        <div class="form-container">
            <form action="/edit-book/<%= book.id %>" method="POST">
                <div class="form-group">
                    <label for="title" class="form-label">Book Title *</label>
                    <input type="text" id="title" name="title" class="form-input" value="<%= book.title %>" required>
                </div>

                <div class="form-group">
                    <label for="author" class="form-label">Author *</label>
                    <input type="text" id="author" name="author" class="form-input" value="<%= book.author %>" required>
                </div>

                <div class="form-group">
                    <label for="cover_url" class="form-label">Cover Image URL</label>
                    <input type="url" id="cover_url" name="cover_url" class="form-input" value="<%= book.cover_url || '' %>">
                </div>

                <div class="form-group">
                    <label for="rating" class="form-label">Rating *</label>
                    <select id="rating" name="rating" class="form-input" required>
                        <option value="5" <%= book.rating == 5 ? 'selected' : '' %>>⭐⭐⭐⭐⭐ (5 stars)</option>
                        <option value="4" <%= book.rating == 4 ? 'selected' : '' %>>⭐⭐⭐⭐ (4 stars)</option>
                        <option value="3" <%= book.rating == 3 ? 'selected' : '' %>>⭐⭐⭐ (3 stars)</option>
                        <option value="2" <%= book.rating == 2 ? 'selected' : '' %>>⭐⭐ (2 stars)</option>
                        <option value="1" <%= book.rating == 1 ? 'selected' : '' %>>⭐ (1 star)</option>
                    </select>
                </div>

                <div class="form-group">
                    <label for="read_date" class="form-label">Date Read</label>
                    <input 
                        type="date" 
                        id="read_date" 
                        name="read_date" 
                        class="form-input" 
                        value="<%= book.read_date ? new Date(book.read_date).toISOString().split('T')[0] : '' %>"
                    >
                </div>

                <div class="form-group">
                    <label for="notes" class="form-label">My Notes & Thoughts</label>
                    <textarea 
                        id="notes" 
                        name="notes" 
                        class="form-textarea"
                    ><%= book.notes || '' %></textarea>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn-submit">Update Book</button>
                    <a href="/" class="btn-cancel">Cancel</a>
                </div>
            </form>
        </div>
    </main>
</body>
</html>
