
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Join Mario's Library Adventure</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body class="auth-body">
    <div class="auth-container">
        <div class="auth-card">
            <h1>🍄 Mario's Library</h1>
            <h2>Start Your Adventure! 🎮</h2>
            <% if (typeof error !== 'undefined') { %>
                <div class="error-message"><%= error %></div>
            <% } %>
            <form action="/register" method="POST" class="auth-form">
                <input type="text" name="username" placeholder="🎮 Your Hero Name" required>
                <input type="email" name="email" placeholder="📧 Your Email (Warp Address)" required>
                <input type="password" name="password" placeholder="🔑 Create Secret Power Code" required>
                <button type="submit" class="btn-primary">🍄 Begin Adventure!</button>
            </form>
            <p class="auth-switch">
                Already have a warp pipe? <a href="/login">🚀 Warp In</a>
            </p>
        </div>
    </div>
</body>
</html>
